export const PayslipTitle = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="ml-8 text-xl underline underline-offset-8" >
            {children}
        </div >
    )
}